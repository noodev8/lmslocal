/*
DISABLED: Manual result management - results now managed via backend fixture service (fixture_load table + push-results-to-competitions API)
This entire page has been disabled. Results are now loaded via the automated fixture service.
To re-enable: uncomment this entire file
*/

// Placeholder to prevent build errors
export default function ResultsPageDisabled() {
  return null;
}
